# Paquete API

- `routers/`: endpoints HTTP.
- `services/`: autenticación, archivos, GPU y ejecución.
- `models.py`: entidades SQLAlchemy.
- `schemas.py`: contratos Pydantic.
- `worker.py`: proceso que ejecuta M5GP en una GPU reservada.
